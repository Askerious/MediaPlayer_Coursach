<Window x:Class="UI.CreatePlaylist"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="CreatePlaylist" Height="360" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70*"/>
            <ColumnDefinition Width="30*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Название плейлиста" Grid.Row="0" Grid.Column="0" Margin="10 5 0 0"/>
        <TextBox Name="PlaylistNameBox" Margin="10 20 10 10" Grid.ColumnSpan="2"/>

        <TextBlock Text="Включить треки" Grid.Row="1" Grid.Column="0" Margin="10 5 0 0"/>
        <ComboBox Name="ImportBox" Grid.Row="1" Grid.ColumnSpan="2" Margin="10 20 10 10"/>

        <Button Click="" Content="Создать" Grid.Row="2" Grid.Column="1" Margin="5"/>
    </Grid>
</Window>
