<Window x:Class="UI.ImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="ImportWindow" Height="120" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70*"/>
            <ColumnDefinition Width="30*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Путь до файла/папки" Grid.Row="0" Grid.Column="0" Margin="10 5 0 0"/>
        <TextBox Name="FileTextBox" Margin="10 20 10 10"/>

        <Button Click="Browse" Content="Обзор" Grid.Row="0" Grid.Column="1" Margin="10 20 10 10"/>

        <TextBlock Text="Импортировать как" Grid.Row="1" Grid.Column="0" Margin="10 5 0 0"/>
        <ComboBox Name="ImportBox" Grid.Row="1" Grid.Column="0" Margin="10 20 10 10"/>

        <Button Click="ImportButton" Content="Импорт" Grid.Row="1" Grid.Column="1" Margin="10 20 10 10"/>
    </Grid>
</Window>
